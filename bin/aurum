#!/usr/bin/env python
import sys
from os.path import abspath, dirname, join
sys.path.append(join(dirname(dirname(abspath(__file__))), "lib"))

from aurum.shell import Shell
shell = Shell()
shell.cmdloop()
